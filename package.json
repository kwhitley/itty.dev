{
  "name": "itty.dev",
  "version": "1.2.1",
  "private": true,
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "postbuild": "cp _worker.js .vitepress/dist",
    "preview": "vitepress preview",
    "release": "release --push --nopublish --patch"
  },
  "type": "module",
  "devDependencies": {
    "sass": "^1.72.0",
    "vitepress": "^1.0.0-rc.42",
    "yarn-release": "^1.10.6"
  },
  "dependencies": {
    "itty-router": "^5.0.9"
  }
}
